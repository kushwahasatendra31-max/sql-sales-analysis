CREATE TABLE sales (
    order_id INT,
    order_date DATE,
    customer_name VARCHAR(100),
    product VARCHAR(100),
    category VARCHAR(50),
    quantity INT,
    sales_amount DECIMAL(10,2),
    city VARCHAR(50)
);
